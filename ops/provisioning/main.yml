- hosts: all
  user: ubuntu
  sudo: yes
  gather_facts: false

  pre_tasks:
    - name: Install Python 2 for ansible
      raw: sudo apt-get -y install python

  tasks:


  roles:
  #  - python
     - nodejs
     - app 

